import { memo } from 'react';
import type { FC } from 'react';
import { PageContainer } from '@ant-design/pro-layout';

const Balance: FC = () => {
  return <PageContainer fixedHeader>haha</PageContainer>;
};

export default memo(Balance);
